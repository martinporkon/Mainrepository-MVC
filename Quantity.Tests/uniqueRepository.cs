using CommonData;
using Quantity.Domain.Common;

namespace Quantity.Tests
{

    internal class uniqueRepository
        //<TObj, TData> : periodRepository<TObj, TData>
        //where TObj : IEntity<TData>
        //where TData : PeriodData, new()
        {

        //protected override string getId(TData d) => d.ValidFrom;
    }

}