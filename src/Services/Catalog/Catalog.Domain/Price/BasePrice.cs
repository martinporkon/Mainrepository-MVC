using Catalog.Data.Price;
using Catalog.Domain.Catalog;
using System;
using System.Collections.Generic;
using System.Text;

namespace Catalog.Domain.Price
{
    //public abstract class BasePrice : DescribedEntity<PriceData>
    //{

    //    protected BasePrice(PriceData d = null) : base(d) { }

    //    protected internal decimal amount => Data?.Amount ?? UnspecifiedDecimal;

    //    protected internal string currencyId => Data?.CurrencyId ?? Unspecified;

    //    //public Money Amount => new Money(amount, currencyId, ValidFrom);

    //}
}
