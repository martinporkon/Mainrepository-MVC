namespace Catalog.Domain.Product
{
    //public sealed class MeasuredProduct : BaseProduct<MeasuredProductType>
    //{

    //    public MeasuredProduct(ProductInstanceData d = null) : base(d) { }

    //    public override MeasuredProductType Type => type as MeasuredProductType;

    //    public string UnitId => Data?.UnitId ?? Unspecified;

    //    public Unit Unit => new GetFrom<IUnitsRepository, Unit>().ById(UnitId);

    //    public double Amount => Data?.Amount ?? UnspecifiedDouble;

    //    public Quantity Quantity => new Quantity(Amount, UnitId);
    //}
}
